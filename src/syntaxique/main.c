#include "../common/jeton.h"

int convertir_en_postfixe(typejeton entree[], typejeton sortie[]) {

  int i;
  int length = sizeof(entree) / sizeof(entree[0]);

  for (i = 0; i < sizeof(entree); i++) {
  };
  // 1. Parcourir 'entree'
  // 2. Vérifier la grammaire (Règles 1 à 6)
  // 3. Remplir 'sortie' dans l'ordre postfixé
  // 4. Retourner un code d'erreur (0 si OK)

  return 67;
};