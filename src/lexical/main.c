#include <stdlib.h>
#include <stdio.h>

#include "lexical.h"

static const char* test_expressions[] = 
{
    "sin(x^2 + 3    - 6)"
};

int main()
{
    for (int i = 0; i < sizeof(test_expressions) / sizeof(const char*); ++i)
    {
        
    }
}